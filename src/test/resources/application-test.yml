# Test profile - disable MCP to avoid requiring Node.js in CI.
# (DataSource auto-config is excluded globally in application.yml so the
# default context loads without a DB; distributed profiles are separate.)
agentscope:
  mcp:
    enabled: false
    demo:
      enabled: false
